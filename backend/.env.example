# Database Configuration
# Neon PostgreSQL connection string
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://username:password@host.neon.tech/dbname?sslmode=require

# Authentication Configuration
# URL of the Better Auth frontend server (for JWKS endpoint)
# The backend fetches public keys from: {BETTER_AUTH_URL}/api/auth/jwks
# Development: http://localhost:3000
# Production: https://your-frontend-domain.com
BETTER_AUTH_URL=http://localhost:3000

# NOTE: Backend does NOT need JWT_SECRET or BETTER_AUTH_SECRET!
# Backend verifies EdDSA tokens using public keys from Better Auth's JWKS endpoint.
# Only the frontend needs BETTER_AUTH_SECRET (for encrypting private keys).

# CORS Configuration
# Comma-separated list of allowed frontend origins
# Development: http://localhost:3000
# Production: https://your-frontend-domain.com
CORS_ORIGINS=http://localhost:3000

# Application Configuration
# Environment: development, staging, production
ENVIRONMENT=development

# API Configuration
API_V1_PREFIX=/api
